// Base URL for backend API, loaded from Vite environment variables
export const API_BASE_URL = import.meta.env.VITE_API_BASE_URL;

// Base URL for OAuth2 authentication provider (e.g., Google, Facebook), also from Vite env
export const OAUTH2_BASE_URL = import.meta.env.VITE_OAUTH2_BASE_URL;
